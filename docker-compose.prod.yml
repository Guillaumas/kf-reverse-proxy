version: '3.8'

services:
  reverse-proxy:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: mspr-kafe-gateway
    restart: unless-stopped
    ports:
      - "8080:8080"
    networks:
      - mspr-network

networks:
  mspr-network:
    driver: bridge
